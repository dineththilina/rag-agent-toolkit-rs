# Rust build artifacts
/target

# Local config with secrets
config.toml

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Legacy BM25 search index (no longer used; kept ignored for old checkouts)
index.json

# Local vector database (SQLite + sqlite-vec), created under data/
data/rag_vectors.sqlite
data/rag_vectors.sqlite-*
*.sqlite-wal
*.sqlite-shm

# Persistent session memory (SQLite), created under data/
data/sessions.sqlite
data/sessions.sqlite-*

# Cached embedding model weights (downloaded on first run by fastembed)
.fastembed_cache/
