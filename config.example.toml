# Copy this to config.toml, or just complete the setup screen in the browser
# (which writes config.toml for you). Every field has a default.

# Any OpenAI-compatible API base URL.
#   OpenAI:    https://api.openai.com/v1
#   Groq:      https://api.groq.com/openai/v1
#   Together:  https://api.together.xyz/v1
#   OpenRouter:https://openrouter.ai/api/v1
#   Ollama:    http://localhost:11434/v1
#   LM Studio: http://localhost:1234/v1
api_base = "https://api.openai.com/v1"

# API key. Leave empty for Ollama / LM Studio.
api_key = ""

# Chat model id (must exist on the chosen API — the UI fetches the live list).
model = "gpt-4o-mini"

# Embeddings. Usually the same API; leave base/key blank to reuse the chat API.
# For chat providers without embeddings (e.g. Groq), point these at OpenAI or
# a local Ollama running an embedding model.
embeddings_base  = ""
embeddings_key   = ""
embeddings_model = "text-embedding-3-small"

# Directory of documents to ingest.
data_dir = "data"

# Note: the vector index is stored in vectors.json (created automatically).
# Delete it to force a rebuild after changing documents.
