// src/api/models.rs — re-exported from api/config.rs; nothing extra needed here.
// The actual handler lives in api::config::get_models; this file is a placeholder
// kept so the module tree is clear about where the route lives.
